# docker-compose.template.yml (TEMPLATE)
#
# This is the TEMPLATE — docker-compose.yml is GENERATED by sync-projects.ts
# and should never be hand-edited. Edit this file instead.
#
# The anchor name must match manifest.compose.anchor (e.g. "my-tool-template").

x-my-tool-template: &my-tool-template
  build:
    context: ../../
    dockerfile: tools/my-tool/Dockerfile
  image: automated-my-tool:latest
  volumes:
    - ./resources:/workspace/tools/my-tool/resources
    - ./projects/${TYPE}/${PROJECT}:/workspace/tools/my-tool/projects/${TYPE}/${PROJECT}
    - ../../outputs/my-tool:/workspace/outputs/my-tool
  working_dir: /workspace/tools/my-tool
  environment:
    - TYPE
    - PROJECT
    - CI=true
    - TZ=Asia/Bangkok
  command: >
    sh -c 'echo "TODO: implement docker runner for ${TYPE}/${PROJECT}"'

services:
  my-tool-web-template-example:
    <<: *my-tool-template
    container_name: my-tool-web-template-example
  # Add your project services here

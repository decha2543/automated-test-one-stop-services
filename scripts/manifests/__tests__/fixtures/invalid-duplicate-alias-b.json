{
  "schemaVersion": "1",
  "id": "beta",
  "alias": "dup",
  "title": "Beta",
  "description": "Individually valid; shares alias 'dup' with alpha to trigger DUPLICATE_ALIAS",
  "version": "1.0.0",
  "enabled": true,
  "runtime": "node",
  "packageManager": "pnpm",
  "taskfile": "Taskfile.yml",
  "projects": {
    "root": "projects",
    "depth": 1,
    "typeAxis": false,
    "fixedType": "performance",
    "templates": { "default": "projects/performance/beta-template-example" },
    "specsSubdir": "automations/specs",
    "sectionAxis": true
  },
  "compose": {
    "template": "docker-compose.template.yml",
    "anchor": "beta-template",
    "networks": []
  },
  "tsconfigGen": null,
  "docker": { "baseImage": "node:22-slim", "extras": ["task", "pnpm"] },
  "runner": {
    "taskNamespace": "beta",
    "title": "Beta",
    "executionTypes": [
      { "id": "run", "title": "Run", "commandTemplate": "{ns}:run-{environment}" }
    ],
    "environments": ["local"],
    "commandTemplate": "task {ns}:run-{environment}",
    "steps": []
  },
  "pipeline": {
    "id": "beta",
    "targetPaths": { "default": "tools/beta/projects/{project}/automations/specs/{name}.spec.ts" },
    "envToken": "process.env.{KEY}",
    "runCommands": { "local": "task beta:run-local PROJECT=<name>" },
    "artifactPaths": ["outputs/beta/{project}/"]
  }
}
